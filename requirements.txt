Flask
flask-cors
Flask-SQLAlchemy
Flask-Login
Flask-Mail
Flask-WTF
WTForms
pandas
openpyxl
pymysql
flask-session
redis
openai
langchain_community
dashscope
langchain_core
langgraph>=0.4.0,<1.1
langgraph-checkpoint-sqlite>=2.0.0,<4
langsmith>=0.3.0,<1
litellm>=1.50.0,<2
python-dotenv
passlib
email-validator
Werkzeug
Jinja2 
boto3
Pillow
prometheus-client
waitress
gunicorn
stripe

# Long-term Memory（mem0 + 本地 Qdrant；ES 仍用于 grep 等工作项索引）
mem0ai>=1.0.0
qdrant-client>=1.12.0
elasticsearch>=8.12.0,<9.0.0

# Text2SQL 依赖
langchain-experimental
langchain-openai
sqlalchemy

# 本机浏览器 CDP 桥服务（local_browser_bridge.py：隧道 Hub + CDP 网关；纯 Python 无编译依赖）
aiohttp>=3.9

# CDP 浏览器工具（agents/cdp/*、agents/tools/*、local_browser_bridge）：走系统 Chrome，
# 不需要 `playwright install` 下载浏览器内核
playwright>=1.40

# 沙箱执行依赖（可选）
# pip install llm-sandbox  # Docker 沙箱执行（推荐）

# Oracle 源库（方案2 预览用，可选）：瘦驱动，无需 Oracle Client，体积小
# pip install oracledb
